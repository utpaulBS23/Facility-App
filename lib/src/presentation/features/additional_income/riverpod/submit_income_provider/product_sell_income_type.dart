// WHY not master-data-sourced: "Product Sell" isn't a real extraEarningType
// row — it's a static local option that switches the form into a
// product-selection flow instead of submitting an income type at all.
const productSellIncomeTypeValue = 'product_sell';
